# rofl-tools

A collection of utilities and quick-deploy tooling.

## Docker Deploys

Ready-to-run dockerized environments. Each deploy is self-contained — just `cd` into the folder and `docker compose up`.

| Deploy | Description |
|--------|-------------|
| [kali-opencode-web](docker-deploys/kali-opencode-web/) | Kali Linux container with OpenCode web IDE, pre-loaded with pentesting tools and headless Chromium |

See [docker-deploys/](docker-deploys/) for the full list.
